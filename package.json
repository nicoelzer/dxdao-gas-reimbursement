{
  "name": "dxdao-gas-reimbursement",
  "version": "0.1.0",
  "main": "getTransactions.js",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node getTransactions && node aggregateTransactions",
    "prettier": "npx prettier --write ."
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "lowdb": "^1.0.0",
    "web3": "^1.2.11"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  }
}
